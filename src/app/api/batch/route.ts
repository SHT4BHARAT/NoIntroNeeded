import { POST as postBatch, GET as getBatch, OPTIONS as optionsBatch } from "@/app/api/v1/batch/route";

export const POST = postBatch;
export const GET = getBatch;
export const OPTIONS = optionsBatch;
