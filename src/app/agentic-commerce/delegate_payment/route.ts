import { OPTIONS as optionsHandler, POST as postHandler, GET as getHandler } from "@/app/agentic_commerce/delegate_payment/route";

export const OPTIONS = optionsHandler;
export const POST = postHandler;
export const GET = getHandler;
