#!/usr/bin/env node
"use strict";

/**
 * `shivanshu` — official CLI alias for the Shivanshu Tiwari portfolio APIs.
 * Thin launcher: the real implementation lives in sht-portfolio-v2 (published
 * on npm), so behavior is identical to `npx sht-portfolio-v2`.
 */
require("sht-portfolio-v2/dist/cli.js");